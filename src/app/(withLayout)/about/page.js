import React from 'react';
export const metadata={
    title: "About Page",
    description: "This is about page"
};

const AboutPage = () => {
    
    return (
        <div>
            <h1>About Page with header and footer</h1>
        </div>
    );
};

export default AboutPage;